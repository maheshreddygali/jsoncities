//This is contributed by ITG 136-137
function getTemp(){
  try{
    
   var dat="sidney"
  switch(dat){
    case "sidney": my(dat)
                   Logger.log(dat)
                   break
                   
    case "perth": my(dat)
                  Logger.log("perth")
                  break
    
    case "Hobart": my(dat)
                   Logger.log("Hobart")
                   break
            
            
    case "Canada": my(dat)
                   Logger.log("canada")
                   break
            
    case "Melbourne" : my(dat)
                       Logger.log("Melbourne")
                       break
            
    case "Kolkata": my(dat)
                    Logger.log("Kolkata")
                    break
  }
  }
catch (error){
Logger.log(error)
}

}
function my(getData) {
  var data =   {"sidney":30, "Perth":40, "Canada":7, "Melbourne":32, "Kolkata":35}
  data=JSON.stringify(data)
  var parsedData = JSON.parse(data);

  Logger.log(parsedData[getData])
}
